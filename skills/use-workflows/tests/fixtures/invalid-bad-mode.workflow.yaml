name: "Invalid Mode Test"
description: "Has invalid execution mode"
version: "1.0.0"

execution_mode: invalid_mode

phases:
  - id: phase1
    name: "Phase 1"
    description: "Test"
    behavior: main-only

finalization:
  template: "Test"
